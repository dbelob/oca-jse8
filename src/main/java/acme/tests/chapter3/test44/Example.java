package acme.tests.chapter3.test44;

public class Example {
    public static final void main(String pieces[]) {
        int characters = 5;
        int story = 3;
        double movieRating = characters <= 4 ? 3 : story > 1 ? 2 : 1;
    }
}